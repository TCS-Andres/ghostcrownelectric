// Placeholder home page. Worker W9 owns the real home page; this only keeps the
// route valid so the site builds during the parallel content build.
export default function HomePage() {
  return (
    <div className="container-page py-24">
      <p className="text-ink-muted">
        Ghost Crown Electric. Home page coming together.
      </p>
    </div>
  );
}
